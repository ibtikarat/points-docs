---
title: 'Postman Collection'
description: 'Ready-to-use Postman collection for Points API v1'
---

<Note>
  A pre-configured Postman collection (with environment variables for `base_url`, `api_key`, `public_key`) is coming soon. It will cover every `/api/v1` endpoint with working example bodies.
</Note>

In the meantime, the OpenAPI spec (`openapi/points-api-v1.yaml`) can be imported directly into Postman — **Import → File** — which generates the same request collection automatically.
